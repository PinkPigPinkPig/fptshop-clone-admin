const { styled, Box } = require('@mui/material');

export const Wrapper = styled(Box)({
  padding: '1.5rem',
  borderRadius: 10,
  backgroundColor: 'white'
});
export const WrapperQuestion = styled(Box)({
  margin: '1rem',
  borderRadius: 4,
  backgroundColor: '#F5F8FF'
});
